# Пример таблицы для make_macros.py (заглушки, не реальные данные).
# Формат: ИМЯ|ДЕЙСТВИЕ|ТЕКСТ
#   text  — Ctrl+A, затем текст
#   cmd   — текст + Enter
#   plain — только текст
# Имя: только A-Z/0-9/_ (оно же показывается на OLED, <=16 символов).

DEMO_HELLO|text|Привет, это тестовый макрос.
DEMO_CMD|cmd|sudo reboot
DEMO_PLAIN|plain|Сервисный недоступен, прошу передать на 2 ЛП.
